[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"
  SECRETS_SCAN_OMIT_KEYS = "ADMIN_SEED_EMAILS,API_ORIGIN"

[[plugins]]
  package = "@netlify/plugin-nextjs"
